def rabbits(n)
  result = 2 ** (n - 1)
end

puts "Enter month"
month = Integer(gets) rescue nil
exit if month.nil?

pairs_rabbits = rabbits( month )
puts "In #{ month } month, #{ pairs_rabbits } pairs"

